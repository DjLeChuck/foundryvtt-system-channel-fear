<div>
  <header class="sheet-header">
    <div class="flexrow">
      <img src="{{item.img}}" data-action="editImage" data-edit="img" title="{{item.name}}" class="image" />
      <h1>
        <input name="name" type="text" value="{{item.name}}" placeholder="{{localize "CF.Global.Name"}}" />
      </h1>
    </div>
  </header>

  <hr />
</div>
